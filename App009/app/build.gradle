plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android'
}

// App 프로젝트를 만드는 방법에 대한 설정
android {
    // base-package
    namespace 'com.callor.game'
    compileSdk 33

    defaultConfig {
        // App 을 배포할때 사용하는 Identifier
        applicationId "com.callor.game"
        minSdk 25
        targetSdk 32
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
    kotlinOptions {
        jvmTarget = '1.8'
    }
    // 안드로이드 스튜디오 4.0 이상에서
    buildFeatures {
        viewBinding true
        dataBinding true
    }

// Android Student 3.x = 4.0 까지 사용까지
//    buildFeatures {
//        viewBinding {
//          enabled = true
//         }
//        dataBinding {
//            enable = true
//        }
//    }

}

dependencies {

    // Kotlin  핵심
    implementation 'androidx.core:core-ktx:1.9.0'

    // Activity 를 만들기위한 대부분의 기능이 구현된 lib
    implementation 'androidx.appcompat:appcompat:1.5.1'

    // Material Theme 를 사용하기 위한 lib
    implementation 'com.google.android.material:material:1.7.0'

    // Constraint Layout 을 사용하기 위한 lib
    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'


    // 단위테이스를 수행하는 도구
    testImplementation 'junit:junit:4.13.2'
    androidTestImplementation 'androidx.test.ext:junit:1.1.4'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.5.0'

}